# from django import forms

# class ReviewForm(forms.Form):
#     rate = forms.ChoiceField(choices=[(i, i) for i in range(1, 6)], label="Rating")
#     title = forms.CharField(max_length=100, label="Review Title")
#     review = forms.CharField(widget=forms.Textarea, label="Review")
